# 2026-09-06（二）：去定妆照 + 定时设备信息刷新

- DisconnectedScreen：删除相机定妆照（camera_body），"未连接设备"与副标题
  textAlign Center 居中（用户要求）。
- ConnectionCenter：新增 startInfoPoll，连接后每 5s 发 INFO 刷新 DeviceStore，
  镜头/电量实时上屏；与心跳共用 ioLock 串行；失败静默重试；断开时取消。
- 装机：adb 3d3390a6 install -r 成功。

# 2026-09-06（三）：顶栏进度重构 + 主页铺满 + 连接流程定版（1.1/vc2）

- 顶栏：ProgressChip → 28dp 圆形进度图标（设置图标左侧，仅活动时出现），
  点击弹悬浮窗显示下载/缩略图进度详情；标题不再被挤压换行。
- SlimProgress 统一去末尾小点（gapSize=0 + drawStopIndicator={}）。
- 主页去定妆照，三卡 24dp 间距纵向居中铺满；镜头卡常驻。
- 发现设备：LazyColumn heightIn(max=320) 修卡片切割；去自动连接改手动选择。
- 扫码连接：suggestion+isAppInteractionRequired 触发系统提示 → 等 WiFi 传输
  通道就绪 → 自动扫描；设备手动选。
- 心跳携带 lens+batteryPct（相机 v1.5），DeviceStore.applyHeartbeat 每次电量
  更新同步镜头。
- adb 装机 3d3390a6 成功。

# 2026-09-06（四）：固件版本显示（1.2/vc3）

- ConnectClient.CameraInfo 增加 firmware 字段（INFO 解析）。
- HomeScreen 序列号与电量之间新增"固件版本"行（用户指定位置）。
- 相机端 v2.0 起 INFO 携带 firmware；adb 装机成功。

# 2026-09-06（五）：悬浮窗/文件页/设置页批量定版 + 传输加固（1.3/vc4）

主界面与悬浮窗：
- 进度悬浮窗钉到**屏幕右缘**、顶栏图标正下方（自定义 PopupPositionProvider
  按窗口宽度定位，不再随 48dp 锚点缩在中间）。
- 进度圈内图标专用化：文件传输=下载箭头矢量，缩略图=新做的"照片+箭头"图标。
- 悬浮窗"已用/剩余/速度"合并为一行三列（省纵向空间不重叠）。
- 删除连接卡 FTP 地址行（挤压"断开连接"按钮）；整页 verticalScroll，
  异形屏/小屏放不下可滚动不再裁字；"已连接"红点改**绿色圆点**。

进度体系：
- SlimProgress 底轨改为**进度色同系浅色**（22% alpha），电量/下载/缩略图/
  顶栏圆圈全部统一。
- 下载进度改为**批次全部文件的总字节进度**（用户指正：此前算成单文件）；
  速度/剩余时间按 **1 秒采样**增量 + EMA 平滑。
- 根因修复：DownloadService 结算时回写循环外陈旧 item，完成文件的
  doneBytes 被覆盖回 0 → "0 B/11.4 MB 已完成"、"下载 0%"、ETA 虚高。
  现在结算一律用本文件最新 lastTotal（完成=size）。

文件页：
- 删除刷新按钮（进目录自动加载）；新增**全选/反选**；面包屑紧凑化腾位。
- 九宫格长按进入选择模式修复（此前 onLongClick 只 toggle 不置 selecting）。
- 选择模式 FAB 改为与传输页一致：开始图标 + "开始传输 (n)"。
- 选集收集（目录递归展开）移到 IO 线程（原先在主线程同步 FTP，大队列 ANR）。

设置页：
- 顶栏图标换**齿轮**矢量；目录显示友好路径（如"内部存储/sony"）。
- **DocSink 修复**：createDocument 此前一律挂树根——文件不进所选目录、
  子目录反复建出 "(1)" 重复目录；现在严格挂目标父目录文档 URI。
- "动态取色（Material You）"改名"莫奈取色"；关于区删 FTP/协议行、版本号
  动态读取、开发者改 **BI2QFA**。

加固（大队列小概率闪退）：
- TransferDrain 线程整体 Throwable 兜底——此前任何未捕获异常直接杀进程；
  现在异常只作废当前批次并完整清理（缩略图恢复、FTP 关闭、标志复位）。
- ThumbStore 拉取线程单条目异常隔离（坏文件不再带死整个队列）；队列
  持久化 runCatching。

图标：应用图标换新素材（五档方+圆，92% 内容留蒙版边距）。

1.3（versionCode 4）构建通过，adb install -r 到 3d3390a6 成功
（dumpsys 确认 versionName=1.3 versionCode=4）。

# 2026-09-06（六）：停止/恢复修复 + 传输页定版 + 修改时间 + 通知动画 + 双网绑定（1.4/vc5）

传输页修复与定版：
- **停止按钮不回弹修复**（两处根因）：①FAB 读的是 DownloadService.isActive()
  普通函数——非响应式，停止后无 recomposition 触发；改读 TransferStore
  .batchRunning（mutableStateOf）。②stopAll 停止后残留 true，再点开始传输
  新排空线程立即退出；ACTION_START 时复位 stopAll=false。
- 点**已完成**项 → 系统选择器打开文件（SAF 目录走文档 URI；应用目录走
  FileProvider，新增 manifest provider + file_paths.xml）；打不开时 Toast 提示。
- "清除已完成"按钮换**倒三角展开菜单**：删除已完成的任务 / **红色**的删除
  所有任务（点后确认弹窗；只删任务记录，绝不动已下载的文件——TransferStore
  .clearAll 新增）。
- 任务旁"移除"改**红色"删除"**。

通知（用户定版）：
- 标题"正在传输"+ 按秒循环省略号动画（. / .. / ... 循环）；副标题 = 总进度
  百分比 + 剩余时间；进度条改**批次总进度**（原为单文件）；收尾显示
  "传输完成 / 传输已停止"。

文件页：列表行与九宫格显示 FTP LIST 自带的**修改时间**
（yyyy-MM-dd HH:mm，目录与文件都显示）。

**Wi-Fi + 移动数据双开连接问题根治**：ConnectionCenter.boundNetwork 从未
被赋值（恒 null），socketFactory 绑定形同虚设——MIUI 把"无互联网"的相机
Wi-Fi 判为劣质网并甩流到蜂窝。现在 pinWifiNetwork()：找到 TRANSPORT_WIFI
的 Network → 注入 FTP/协议客户端 socketFactory + **bindProcessToNetwork**
（覆盖扫描裸 Socket）；扫描/扫码/连接三处入口都钉；断开时 unpin 恢复默认。
ConnectionCenter.init 增加 context 参数。

1.4（versionCode 5）构建通过，adb install -r 成功（dumpsys 确认 1.3→1.4）。

# 2026-09-06（七）：续传超量修复 + 优先连接设备 + 自动扫描 + 去扫码（1.5/vc6）

- **续传超量修复（39.8MB/23.9MB）**：pumpToStream 的 onProgress 回调值本就
  是绝对字节数（REST 基址+已传），DownloadService 又加了一次 base——从断点
  续传起显示翻倍。回调直接当绝对值用；TransferStore.updateProgress 与传输页
  显示均按文件大小钳制（旧版持久化的超量值自愈）。
- 传输列表为空时不显示开始传输 FAB。
- 设置页下载目录下新增**优先连接设备**：说明文案"扫描到选中的设备时自动
  连接"；信息外露式选择框，默认"未选择"，下拉列出连接过的设备
  （"ILCE-6300 SN:05186914" 格式，SettingsRepo.deviceHistory 持久化，
  连接成功即记录，最多 10 条）。
- **自动扫描**：进未连接页即开始扫描，3 秒一轮持续扫到设备为止；优先设备
  型号+序列号全匹配时**免手动直接自动连接**（Scanner.Candidate 新增 serial，
  connectTo 成功后 recordDevice）。扫到但未命中则停下等手选；"扫描设备"
  按钮重开一轮。connecting 标志防止连接握手与扫描循环打架。
- **扫码连接整体移除**：DisconnectedScreen 扫码按钮与流程、
  QrScanActivity、QrWifiParser、其单测、zxing 依赖、CAMERA 权限全删。
- 文案："与相机失去联系"→"失去与相机的连接"；设置页删除底部
  "断开与相机的连接"按钮（断开走主界面）。

1.5（versionCode 6）构建通过，adb install -r 成功。

# 2026-09-06（八）：返回键层级化 + 持续扫描形态定版（1.6/vc7）

- **返回键层级化**（此前按返回直接回桌面）：
  设置页打开 → 返回=关设置；文件/传输 tab → 返回=回主界面 tab；
  文件页中 → 返回=上一层目录（FtpRepository.parentOf）；
  选择模式中 → 返回=先退出选择模式（后注册的 BackHandler 优先）；
  只有在顶层（未连接页/主界面）返回才交给系统退出。
- **持续扫描形态**（用户定版）：进软件即进入持续扫描状态——单一 scanning
  状态全程保持（按钮文案恒定"扫描设备"，不再每轮闪烁"扫描中"），按钮下方
  常驻"正在扫描网络…"小指示；**超过 30 秒**未扫到 → 显示"未找到设备"并
  停止自动扫描（手动"扫描设备"可重开，重开重新计时）。

1.6（versionCode 7）构建通过，adb install -r 成功。

# 2026-09-06（九）：扫描状态回按钮内 + 文案（1.7/vc8）

- 扫描中状态改回按钮内：转圈 + "扫描中"动画点（1s 循环 . .. ...，与通知
  一致）；30 秒持续扫描逻辑不变。
- "（相机热点或同一路由器）"→"（相机热点或同一局域网内）"。

1.7（versionCode 8）构建通过，adb install -r 成功。

# 2026-09-06（十）：传输页闪退修复 + FAB/悬浮窗定版（1.8/vc9）

- **传输页闪退根因修复**：logcat 实锤 LazyColumn 重复 key 崩溃——enqueue
  允许对已完成/失败任务再次入队，同 id 出现两条。改为同 id 原位重排为等待
  （进度保留）；load() 对旧持久化数据去重（手机上已存在的脏数据自愈）。
- FAB：全部任务已完成时也隐藏（此前只隐藏空列表）。
- 悬浮窗：缩略图加载完成显示"缩略图加载完成 n/n"，完成态不再带"已暂停"。

# 2026-09-06（十一）：下载目录真凶修复 + 浏览位置记忆（1.9/vc10）

- **自定义下载目录不生效的根因（NPE 实锤）**：DocSink 的 dirs/partDoc/
  finalDoc 三个属性声明在 init 块之后——Kotlin 按声明顺序初始化，init 跑时
  它们还是 null，构造必然 NPE → 静默回退应用目录。真机 UI 自动化（设置→
  选择目录→授权→传输）抓到栈。修复：属性移到 init 之前声明；回退处加
  日志（StorageSink tag，不再静默）。
- 文件页浏览位置提升到 AppRoot 持有：切到主界面/传输再回来仍在原目录。
- 设置页下载目录显示友好路径（"内部存储/sony"）真机验证通过。

# 2026-09-06（十二）：后台断连修复（1.10/vc11）

- 切后台断连两处根因：①连接期间无常驻前台服务，MIUI 冻结/限网；②心跳
  一次失败立即断开。修复：新增 ConnectionService（前台 dataSync，连接成功
  启动/断开停止，常驻低优先级通知）；心跳改**连续 3 次**失联才判断开
  （协议设计原始定版），瞬断自动恢复。
- 已在 87d9369f（24129PN74C）装机验证服务注册；3d3390a6 离线，待重连后补装。

# 2026-09-06（十三）：传输完成自动退出相机（1.11/vc12，配相机 v2.4）

- 设置页优先连接设备下方新增开关"传输完成后自动关闭相机客户端"（默认关，
  SettingsRepo.autoExitAfterTransfer 持久化）。
- 协议新增 EXIT_APP：批次自然完成（非手动停止）且开关打开时
  DownloadService 发送 ConnectClient.exitApp()；相机收到走与手动退出完全
  一致的收尾，仅弹窗文字为"传输完成，正在自动退出···"。

1.11（versionCode 12）构建通过，87d9369f 装机成功。

# 2026-09-06（十四）：过渡动画 + 首启通知权限 + 通知定版 + 进度归零 + 版本归一（1.0/vc1）

- **页面切换过渡动画**：AppRoot 顶层 AnimatedContent——新页淡入+轻微右滑入
  （220ms），旧页淡出（140ms）；设置/未连接页/三个 tab 全部生效。
- **首次进入软件先请求通知权限**：MainActivity onCreate 里一次性发起
  POST_NOTIFICATIONS（sharedPrefs 记 asked 标记，拒绝后传输页仍会再请求）。
- 去掉 DocSink 回退处临时加的 Log 日志（恢复静默回退，功能不变）。
- 冗余清理：删除无调用的 ConnectionCenter.dismissError、
  TransferStore.totalOfPending、无引用 drawable ic_nav_settings。
- **保活通知文案**（用户定版）："与 xx 保持连接"→"xx 已连接"；
  保活/进度/结束三类通知全部加了 contentIntent——**点击直接进入软件**。
- **批次总进度从 0% 开始**（用户反馈不归零）：进度语义改为"本批新下载字节
  / 本批剩余量"——断点续传的已有字节作为入队基线（batchBase）不计入；
  已完成任务重新入队时进度归零（.part 已提交不存在），失败重试保留断点。
- **传输结束独立通知**（可划走）：标题"传输结束"，内容"完成 n · 失败 n"，
  批次收尾（含异常兜底路径）统一发出。

**版本号归一：1.0（versionCode 1）**（用户定版）。已 adb install -r -d
降级覆盖到 87d9369f（dumpsys 确认 versionName=1.0）。

# 2026-09-06（十五）：过渡动画改纯淡入淡出

- AnimatedContent 去掉右滑入分量，改为纯 fadeIn(220ms)/fadeOut(140ms)
  （用户定版）。版本号维持 1.0 / versionCode 1 不变，已装机。

# 2026-09-06（十六）：大队列两个致命 bug 修复（版本维持 1.0/vc1）

- **批次总量 >2GB 通知进度条崩溃**（361 个任务约 4.3GB 必炸，~86 个以内
  不炸）：buildNotification 里 max.toInt() 截断成负数 → coerceIn 抛
  IllegalArgumentException → drainSafely 兜底 → "点开始传输立刻自动结束"。
  修复：总量超 Int 上限时按比例缩放到 Int.MAX_VALUE，进度条视觉不变。
  真机 logcat 实锤栈。
- **每 commit 一个文件就终止整批**（MIUI SAF）：.ARW 的 mime 用了
  image/x-sony-arw，MIUI 提供器不认 → 建档时强改文件名（.part →
  .part.arw，重试堆积垃圾）→ renameDocument 失败 → 拷贝改名兜底里
  openInputStream 报 Missing file 异常 → 从 commit 直接穿出循环炸批次
  （logcat 实锤 5 连崩）。修复：①ARW 的 mime 改 application/octet-stream
  （无后缀规范化，MIUI 不改写名字）；②commit/copyRename 全防弹（异常不
  外抛）；③drainQueue 把 commit 纳入单文件容错（失败标 FAILED 重试）；
  ④drainSafely 收回 RUNNING 后补 persistNow 落盘。
- 真机验证：361 任务队列 45 秒零崩溃，DONE 4→12 连续推进。
- 用户注意：下载目录 SonyConnect/DCIM/100MSDCF/ 里此前崩溃残留的
  *.part.arw 垃圾文件可手动删除。

# 2026-09-06（十七）：删除任务即停传输 + 选择模式/预览交互定版（版本维持 1.0/vc1）

- 下载中"删除所有任务"：clearAll 后立刻 DownloadService.stopCurrent——
  当前传输立即停止（此前会空转到当前文件下完、进度条归零假跑）。
- 确认弹窗文案："将移除全部传输任务记录"→"将删除全部传输任务记录"。
- 九宫格选中角标：裸对号改为**实心圆底（primary）+ 白色对号**，缩略图上清晰可辨。
- 全屏预览：**单击屏幕任意处退出**（此前只能按返回）。
- 长按语义（用户定版）：未在选择模式→长按进入并选中该项；已在选择模式→
  **长按退出选择模式**（列表与九宫格一致）。返回键退出选择保留。
- 构建通过（aapt 复核 1.0/1），手机离线未装机，重连后 install -r 即可。

# 2026-09-06（十八）：文件页怪红字根治（版本维持 1.0/vc1）

- 红字来源锁定：文件页 loadError 直接显示 e.message 原文——浏览中切页/
  换目录时 LaunchedEffect 被取消，CancellationException（message 含协程
  scope 字样）被 catch(Exception) 当成目录读取错误上屏。
- 修复：CancellationException 放行不当错误；其余异常统一显示固定
  "目录读取失败"，原始原因只进 Log.d（排查用）。全应用不再有任何把原始
  异常文本直接上屏的红字槽位。

# 2026-09-06（十九）：关于区配套说明带版本号（版本维持 1.0/vc1）

- 设置页关于："配套相机端 SonyConnect 使用"→"配套相机端 SonyConnect 1.0 使用"。
- 相机端关于页同步新增版本行（见相机端 devlog）。

# 2026-09-06（二十）：删除确认文案精确化（版本维持 1.0/vc1）

- 删除所有任务确认弹窗："不会删除已下载的文件"→"不会删除本机已下载的文件"。

# 2026-09-06（二十一）：通知点击不再开出第二实例（版本维持 1.0/vc1）

- MainActivity 设 launchMode="singleTask"：点击通知复用现有任务栈
  （此前应用在前台时点通知会压一个新的 MainActivity 实例，返回栈出现
  两层界面）。
