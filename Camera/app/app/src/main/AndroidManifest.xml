<?xml version="1.0" encoding="utf-8"?>
<!-- 版本号只在 app/build.gradle 的 defaultConfig 里维护（Gradle 会覆盖此处的值，
     写在这里只会让读的人误判当前版本，所以刻意不写）。 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bi2qfa.sonyconnect">

    <uses-sdk android:minSdkVersion="10" android:targetSdkVersion="10" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@android:style/Theme.Black.NoTitleBar">

        <!-- 相机平台会话结束广播：Activity.onDestroy 可能不执行，这里是最后兜底 -->
        <receiver android:name=".ExitCompletedReceiver">
            <intent-filter>
                <action android:name="com.android.server.DAConnectionManagerService.ExitCompleted" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- 静态分析定版：PMCA 应用单进程铁律——不再有任何探针服务/子进程/原生库 -->

        <activity android:name=".MainActivity"
            android:screenOrientation="landscape"
            android:configChanges="orientation|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
